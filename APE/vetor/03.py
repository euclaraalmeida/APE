total=int(input(''))
vetor=[None]*total
k=int(input(''))
for indice in range(total):
    vetor[indice]=int(input(''))
    if vetor[indice]==k:
        print(f"{k} esta na posição {indice} ")
print(f"{k} esta na posição {indice} ")
